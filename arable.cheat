% arable
# Login to the ALT console
aws-vault login arable-alt-admin

# Login to the Production console
aws-vault login arable-prod-admin

# Run command on ALT
aws-vault exec arable-alt-admin -- 

# Run command on Production
aws-vault exec arable-prod-admin -- 

# Create alembic migration
alembic revision -m "<message>"

# Dummy item at the end
aws-vault login arable-prod-admin