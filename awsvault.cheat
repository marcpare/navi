% aws, aws-vault
# Command Store AWS credentials for a profile
aws-vault add <profile>

# Command Execute a command (using temporary credentials)
aws-vault exec <profile> --

# Command Open a browser window and login to the AWS Console
aws-vault login <profile>

# Command List credentials
aws-vault list

% git

# Set local git user email
git config --local user.email <email>
