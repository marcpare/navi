% python

# Create a new virtual environment
python3 -m venv ~/venvs/<name>

# Create a py27 virutal environment
virtualenv --python=/usr/bin/python2.7 ~/venvs/<name>

# Dummy item at the end
echo "foo"
